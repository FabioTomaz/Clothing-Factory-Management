<Page x:Class="Trabalho_BD_IHC.ListarEmpregados"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Trabalho_BD_IHC"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="ListarEmpregados" Loaded="Page_Loaded">

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="24*"/>
            <ColumnDefinition Width="142*"/>
            <ColumnDefinition Width="16*"/>
            <ColumnDefinition Width="473*"/>
            <ColumnDefinition Width="23*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.05*"/>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition Height="0.1*"/>
        </Grid.RowDefinitions>
        <GroupBox Header="Operações" Grid.Column="1" Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.1*"></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition Width="0.1*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="0.3*"></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition Height="0.3*"></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition Height="0.3*"></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition Height="0.3*"></RowDefinition>
                </Grid.RowDefinitions>
                <Button x:Name="registarEmpregado" Content="Registar Empregado" Grid.Row="1" Grid.Column="1" Click="registarEmpregado_Click"/>
                <Button x:Name="editarEmpregado" Content="Editar Empregado" Grid.Row="3" Grid.Column="1" IsEnabled="False"/>
                <Button x:Name="removerEmpregado" Content="Remover Empregado" Grid.Row="5" Grid.Column="1" IsEnabled="False" Click="removerEmpregado_Click"/>
            </Grid>
        </GroupBox>
        <Grid Grid.Row="2" Grid.Column="1">
            <Label Grid.Row="2" Grid.Column="1" Content="Clique duas vezes num funcionário para ver mais informações sobre ele." ></Label>
        </Grid>
        <Grid Grid.Column="3" Grid.Row="1" Grid.RowSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.1*"></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.Resources>
                <Style x:Key="informationStyle" TargetType="{x:Type ListViewItem}">
                    <EventSetter Event='MouseDoubleClick' Handler='empregado_DoubleClick'/>
                </Style>
            </Grid.Resources>
            <ListView  Name="empregados" Grid.Row="1" SelectionChanged="empregados_SelectionChanged" ItemContainerStyle="{StaticResource informationStyle}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Função" DisplayMemberBinding="{Binding TipoUser}" />
                        <GridViewColumn Header="Nº Empregado"  DisplayMemberBinding="{Binding NFuncionario}" />
                        <GridViewColumn Header="Nome" DisplayMemberBinding="{Binding Nome}" />
                        <GridViewColumn Header="E-mail" DisplayMemberBinding="{Binding Email}" />
                        <GridViewColumn Header="Telefone" DisplayMemberBinding="{Binding Telemovel}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Grid>
</Page>
